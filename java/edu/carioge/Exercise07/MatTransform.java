package edu.carioge.Exercise07;
/*
public class MatTransform {
    public double [] matmul(double [][] M, double [] v) {
        if(M == null || M.length == 0) {
            System.err.println("M must be non-null and must have...");
            return null;
        }

        if(M.length != M[0].length) {
            System.err.println("M must be squard!");
            return null;
        }

        if(v == null || v.length == 0) {
            System.err.println("v invalid!");
        }
    }
    public static void main(String [] args) {

    }
}
 */
